import React from 'react';
import newIconPng from '../pngs/member_icon.png'; // Ensure the correct path and module declaration

const NewIcon: React.FC = () => (
    <img src={newIconPng} alt="New Icon" />
);

export default NewIcon;
